{
  "name": "Lint on Save",
  "version": "1.0.0",
  "description": "Run ESLint on saved TypeScript files to catch issues early.",
  "when": {
    "type": "fileEdited",
    "patterns": ["*.ts", "*.tsx"]
  },
  "then": {
    "type": "runCommand",
    "command": "npx eslint --fix ${file}"
  }
}
